name: PR-Linter

on:
  pull_request:

jobs:
  PR-Linter:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2  
      - name: Python Linter
        uses: sunnysid3up/python-linter@master
